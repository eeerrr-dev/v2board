dist
node_modules
*.min.*
pnpm-lock.yaml
# Golden wire fixtures are generated byte-for-byte by the Rust golden lane
# (`make contract-goldens`); prettier must not fight the generator.
packages/api-client/goldens

# Deterministic projections of the Rust OpenAPI source. Their generator owns
# byte layout because `make api-contract-check` compares them byte-for-byte.
packages/api-client/openapi/internal-api.openapi.json
packages/api-client/src/generated/internal-api.ts
packages/types/src/generated/internal-api.ts
